{
  "mcpServers": {
    "gemini-omni": {
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/gemini-omni-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
